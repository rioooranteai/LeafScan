class RiwayatController {
  void lihatRiwayat() {
    // Logika untuk melihat riwayat deteksi
  }

  void lihatDetailRekomendasi(String idDeteksi) {
    // Logika untuk melihat detail rekomendasi berdasarkan idDeteksi
  }
}
